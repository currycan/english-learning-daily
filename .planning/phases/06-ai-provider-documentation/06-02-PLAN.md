---
phase: 06-ai-provider-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/configuration.md
  - docs/setup-guide.md
autonomous: true
requirements:
  - DOCS-03

must_haves:
  truths:
    - "docs/configuration.md GitHub Secrets table lists OPENAI_API_KEY alongside BARK_TOKEN and ANTHROPIC_API_KEY"
    - "docs/setup-guide.md Step 7 or adjacent section links readers to docs/ai-providers.md for dual-provider setup"
  artifacts:
    - path: "docs/configuration.md"
      provides: "Updated GitHub Secrets table with OPENAI_API_KEY row"
      contains: "OPENAI_API_KEY"
    - path: "docs/setup-guide.md"
      provides: "Cross-link to docs/ai-providers.md from API key setup section"
      contains: "ai-providers.md"
  key_links:
    - from: "docs/setup-guide.md"
      to: "docs/ai-providers.md"
      via: "Markdown link"
      pattern: "ai-providers\\.md"
    - from: "docs/configuration.md"
      to: "OPENAI_API_KEY secret"
      via: "secrets table row"
      pattern: "OPENAI_API_KEY"
---

<objective>
Update docs/configuration.md and docs/setup-guide.md to reflect the dual-provider reality introduced in Phases 4-5.

Purpose: A developer following configuration.md or setup-guide.md will now find OPENAI_API_KEY mentioned correctly and will be directed to docs/ai-providers.md for detailed setup. Without this, the secrets table in configuration.md is incomplete and setup-guide.md only mentions the Anthropic key.
Output: Updated docs/configuration.md (OPENAI_API_KEY added to secrets table), updated docs/setup-guide.md (cross-link to ai-providers.md added)
</objective>

<execution_context>
@/Users/andrew/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrew/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-ai-provider-documentation/06-RESEARCH.md

<interfaces>
<!-- Current state of files being modified. Executor must read these before touching anything. -->

From docs/configuration.md — current GitHub Secrets table (lines 103-107):
```markdown
| Secret / 密钥 | Required By / 所需系统 | Description / 说明 |
|---------------|----------------------|-------------------|
| `BARK_TOKEN` | Push notification system / 推送通知系统 | Bark app token (token only, no URL prefix) / Bark App token（仅 token，不含 URL 前缀） |
| `ANTHROPIC_API_KEY` | Content generation system / 内容生成系统 | Anthropic API key for Claude / Claude 的 Anthropic API key |
```
OPENAI_API_KEY row is MISSING — must be added.

From docs/setup-guide.md — Step 6 and Step 7 (lines 80-100):
```markdown
### Step 6 — Get an Anthropic API Key / 第六步：获取 Anthropic API Key

Sign up at [console.anthropic.com](https://console.anthropic.com) and create an API key.

### Step 7 — Add ANTHROPIC_API_KEY secret / 第七步：添加 ANTHROPIC_API_KEY Secret
```
No mention of OpenAI key or link to ai-providers.md — must add cross-link.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OPENAI_API_KEY row to docs/configuration.md secrets table</name>
  <files>docs/configuration.md</files>
  <read_first>
    - docs/configuration.md (full file — read entire file before editing; locate the GitHub Secrets table to know exact insertion point)
  </read_first>
  <action>
Read docs/configuration.md in full. Find the GitHub Secrets table (under `## GitHub Secrets / GitHub 密钥`, around line 103). The table currently has two data rows: BARK_TOKEN and ANTHROPIC_API_KEY.

Add a third row for OPENAI_API_KEY. Insert it AFTER the ANTHROPIC_API_KEY row. Use the exact same column format as the existing rows:

```markdown
| `OPENAI_API_KEY` | Content generation system / 内容生成系统 | OpenAI API key for GPT-4o-mini / GPT-4o-mini 的 OpenAI API key |
```

Do not change any other part of the file. Do not reformat the table. The Security rules block beneath the table remains unchanged.
  </action>
  <verify>
    <automated>grep -n 'OPENAI_API_KEY' /Users/andrew/study-all/docs/configuration.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'OPENAI_API_KEY' docs/configuration.md` outputs exactly one line containing the table row
    - The line contains all three columns separated by `|`: the secret name, "Content generation system", and a description
    - The ANTHROPIC_API_KEY row is still present (not replaced)
    - `grep -c '|' docs/configuration.md` count is unchanged except for the one new row added
  </acceptance_criteria>
  <done>docs/configuration.md GitHub Secrets table has three data rows: BARK_TOKEN, ANTHROPIC_API_KEY, OPENAI_API_KEY. Running `grep 'OPENAI_API_KEY' docs/configuration.md` returns exactly one matching line in the table.</done>
</task>

<task type="auto">
  <name>Task 2: Add cross-link to docs/ai-providers.md from docs/setup-guide.md</name>
  <files>docs/setup-guide.md</files>
  <read_first>
    - docs/setup-guide.md (full file — read entire file before editing; locate Step 7 and the area after it to find the exact insertion point)
  </read_first>
  <action>
Read docs/setup-guide.md in full. Find Step 7 (`### Step 7 — Add ANTHROPIC_API_KEY secret`). After Step 7's closing content (after the Chinese line "该 key 注入到 CI 环境中——**绝不**存储在代码中。"), add the following new block before Step 8:

```markdown
### Step 7a — For OpenAI + dual-provider setup / 第 7a 步：OpenAI 双提供商配置（可选）

For dual-provider setup (OpenAI + Anthropic with automatic fallback), see [`docs/ai-providers.md`](./ai-providers.md).

如需配置 OpenAI + Anthropic 双提供商（含自动降级），请参阅 [`docs/ai-providers.md`](./ai-providers.md)。
```

Do not renumber existing steps. Do not change any other content. The insertion goes between Step 7's last line and Step 8's heading.
  </action>
  <verify>
    <automated>grep -n 'ai-providers.md' /Users/andrew/study-all/docs/setup-guide.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'ai-providers.md' docs/setup-guide.md` returns at least one line (the cross-link)
    - The line contains a Markdown link: `[` followed by `ai-providers.md` followed by `]`
    - Steps 1-8 headings all remain present (use `grep -c '### Step' docs/setup-guide.md` — should return 8 original steps plus the new 7a = 9 total)
    - The file still contains "Step 8" heading (not accidentally deleted)
  </acceptance_criteria>
  <done>docs/setup-guide.md contains a Markdown link to ai-providers.md. Running `grep 'ai-providers.md' docs/setup-guide.md` returns at least one line. `grep '### Step' docs/setup-guide.md` returns 9 lines (original 8 steps plus new 7a).</done>
</task>

</tasks>

<verification>
Smoke checks after both tasks:

```bash
grep -q 'OPENAI_API_KEY' /Users/andrew/study-all/docs/configuration.md && echo "CONF OK"
grep -q 'ai-providers.md' /Users/andrew/study-all/docs/setup-guide.md && echo "SETUP OK"
```

Full test suite must still pass (no regressions from doc edits):

```bash
cd /Users/andrew/study-all && pytest tests/ -q
```
</verification>

<success_criteria>
- docs/configuration.md GitHub Secrets table contains OPENAI_API_KEY row
- docs/setup-guide.md contains a cross-link to docs/ai-providers.md
- pytest tests/ passes with no regressions
- No other content in either file changed
</success_criteria>

<output>
After completion, create `.planning/phases/06-ai-provider-documentation/06-02-SUMMARY.md`
</output>
