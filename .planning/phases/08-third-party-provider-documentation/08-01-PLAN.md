---
phase: 08-third-party-provider-documentation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_ai_provider_docs.py
  - docs/ai-providers.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03

must_haves:
  truths:
    - "docs/ai-providers.md contains a Section 5 with bilingual (English + Chinese) content about third-party Claude-compatible API configuration"
    - "docs/ai-providers.md config.json example shows anthropic_base_url and anthropic_auth_token fields merged with existing config fields"
    - "docs/ai-providers.md GitHub Secrets sub-section inside Section 5 shows ANTHROPIC_BASE_URL and ANTHROPIC_AUTH_TOKEN with the standard Settings navigation code block format"
    - "Summary table at the end of docs/ai-providers.md has two new optional rows for ANTHROPIC_BASE_URL and ANTHROPIC_AUTH_TOKEN"
    - "All pytest tests in tests/test_ai_provider_docs.py pass (including new tests for DOCS-01/02/03 v1.2)"
  artifacts:
    - path: "docs/ai-providers.md"
      provides: "Third-party provider setup guide"
      contains: "## 5. Third-Party Claude API"
    - path: "tests/test_ai_provider_docs.py"
      provides: "Automated doc coverage assertions"
      exports: ["test_section5_exists", "test_config_example_fields", "test_github_secrets_section"]
  key_links:
    - from: "tests/test_ai_provider_docs.py"
      to: "docs/ai-providers.md"
      via: "DOCS_FILE.read_text() in docs_content fixture"
      pattern: "DOCS_FILE.read_text"
---

<objective>
Extend docs/ai-providers.md with a new Section 5 covering third-party Claude-compatible API configuration, and add failing test stubs first (TDD RED) then implement the documentation (TDD GREEN).

Purpose: Users setting up a third-party Claude-compatible API endpoint can follow the documentation end-to-end without reading source code.
Output: Updated docs/ai-providers.md with Section 5 + two Summary table rows; updated tests/test_ai_provider_docs.py with passing tests for DOCS-01/02/03 v1.2 requirements.
</objective>

<execution_context>
@/Users/andrew/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrew/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-third-party-provider-documentation/08-CONTEXT.md
@.planning/phases/08-third-party-provider-documentation/08-RESEARCH.md

<interfaces>
<!-- Key patterns extracted from codebase. Executor MUST follow these exactly. -->

From docs/ai-providers.md (Section 3) — GitHub Secrets code block format:
```markdown
```
Settings → Secrets and variables → Actions → New repository secret
  Name:  OPENAI_API_KEY
  Value: <your OpenAI API key>
```
```

From docs/ai-providers.md (Section 4) — JSON config code block format:
```json
{
  "ai_provider": "anthropic",
  "openai_model": "gpt-4o-mini"
}
```

From docs/ai-providers.md (Section 4) — numbered section heading format:
```markdown
## 4. Provider Priority Rule / 提供商优先级规则
```

From docs/ai-providers.md — Summary table format:
```markdown
| Secret / 密钥 | Required By / 所需功能 |
|---|---|
| `ANTHROPIC_API_KEY` | Default provider (`anthropic`); fallback when OpenAI is primary |
| `OPENAI_API_KEY` | Required when `AI_PROVIDER=openai` or as fallback for Anthropic |
```

From tests/test_ai_provider_docs.py — existing test pattern:
```python
def test_openai_secret_documented(docs_content: str):
    assert "OPENAI_API_KEY" in docs_content, (
        "DOCS-03: docs/ai-providers.md must contain 'OPENAI_API_KEY'"
    )
```

From scripts/ai_provider.py (priority chain verified):
```python
effective_url = os.environ.get("ANTHROPIC_BASE_URL") or base_url or ""
effective_key = os.environ.get("ANTHROPIC_AUTH_TOKEN") or auth_token or ""
```

From scripts/generate_exercises.py (config field names verified):
```python
"anthropic_base_url": config.get("anthropic_base_url"),
"anthropic_auth_token": config.get("anthropic_auth_token"),
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing test stubs for DOCS-01/02/03 v1.2 (TDD RED)</name>
  <files>tests/test_ai_provider_docs.py</files>
  <read_first>
    - tests/test_ai_provider_docs.py — read full current content before appending; understand existing fixture and test naming conventions
    - docs/ai-providers.md — confirm the exact strings that will NOT yet exist (so tests fail correctly in RED state)
  </read_first>
  <behavior>
    - test_section5_exists: assert "## 5. Third-Party Claude API" in docs_content — FAILS until Section 5 is written
    - test_third_party_bilingual: assert "第三方 Claude 兼容 API" in docs_content — FAILS until Chinese translation is present
    - test_config_example_fields: assert "anthropic_base_url" in docs_content AND "anthropic_auth_token" in docs_content — FAILS until config block is written
    - test_github_secrets_section: assert "ANTHROPIC_BASE_URL" in docs_content AND "ANTHROPIC_AUTH_TOKEN" in docs_content — FAILS until Section 5 secrets block is written
    - test_summary_table_optional_rows: assert "ANTHROPIC_BASE_URL (optional)" in docs_content AND "ANTHROPIC_AUTH_TOKEN (optional)" in docs_content — FAILS until Summary rows are appended
    - test_priority_stated: assert "highest priority" in docs_content OR "env var" in docs_content.lower() — FAILS until priority text is written (note: this may already pass due to existing content; adjust assertion to check within Section 5 context if needed — check by asserting that the string appears after the Section 5 heading)
  </behavior>
  <action>
    Append the following new test functions to tests/test_ai_provider_docs.py after the existing tests. Do NOT modify existing tests. The module-level `docs_content` fixture is already defined and will be reused automatically.

    Add this block at the end of the file:

    ```python
    # ---------------------------------------------------------------------------
    # v1.2 DOCS-01 — Third-party Claude API section (bilingual)
    # ---------------------------------------------------------------------------


    def test_section5_exists(docs_content: str):
        assert "## 5. Third-Party Claude API" in docs_content, (
            "DOCS-01: docs/ai-providers.md must contain '## 5. Third-Party Claude API'"
        )


    def test_third_party_bilingual(docs_content: str):
        assert "第三方 Claude 兼容 API" in docs_content, (
            "DOCS-01: docs/ai-providers.md must contain Chinese translation '第三方 Claude 兼容 API'"
        )


    # ---------------------------------------------------------------------------
    # v1.2 DOCS-02 — config.json example with anthropic_base_url / anthropic_auth_token
    # ---------------------------------------------------------------------------


    def test_config_example_fields(docs_content: str):
        assert "anthropic_base_url" in docs_content, (
            "DOCS-02: docs/ai-providers.md must contain 'anthropic_base_url' in config example"
        )
        assert "anthropic_auth_token" in docs_content, (
            "DOCS-02: docs/ai-providers.md must contain 'anthropic_auth_token' in config example"
        )


    # ---------------------------------------------------------------------------
    # v1.2 DOCS-03 — GitHub Secrets section for ANTHROPIC_BASE_URL / ANTHROPIC_AUTH_TOKEN
    # ---------------------------------------------------------------------------


    def test_github_secrets_section(docs_content: str):
        assert "ANTHROPIC_BASE_URL" in docs_content, (
            "DOCS-03: docs/ai-providers.md must contain 'ANTHROPIC_BASE_URL'"
        )
        assert "ANTHROPIC_AUTH_TOKEN" in docs_content, (
            "DOCS-03: docs/ai-providers.md must contain 'ANTHROPIC_AUTH_TOKEN'"
        )


    def test_summary_table_optional_rows(docs_content: str):
        assert "ANTHROPIC_BASE_URL (optional)" in docs_content, (
            "DOCS-03: Summary table must contain 'ANTHROPIC_BASE_URL (optional)'"
        )
        assert "ANTHROPIC_AUTH_TOKEN (optional)" in docs_content, (
            "DOCS-03: Summary table must contain 'ANTHROPIC_AUTH_TOKEN (optional)'"
        )
    ```

    After appending, run `pytest tests/test_ai_provider_docs.py -v` and confirm:
    - test_section5_exists FAILS (RED)
    - test_third_party_bilingual FAILS (RED)
    - test_config_example_fields FAILS (RED)
    - test_github_secrets_section FAILS (RED)
    - test_summary_table_optional_rows FAILS (RED)
    - All pre-existing tests PASS (GREEN — they must not be broken)
  </action>
  <verify>
    <automated>pytest tests/test_ai_provider_docs.py -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_ai_provider_docs.py contains `def test_section5_exists(`
    - tests/test_ai_provider_docs.py contains `def test_third_party_bilingual(`
    - tests/test_ai_provider_docs.py contains `def test_config_example_fields(`
    - tests/test_ai_provider_docs.py contains `def test_github_secrets_section(`
    - tests/test_ai_provider_docs.py contains `def test_summary_table_optional_rows(`
    - `pytest tests/test_ai_provider_docs.py -v` output shows the 5 new tests as FAILED
    - `pytest tests/test_ai_provider_docs.py -v` output shows all 7 pre-existing tests as PASSED
  </acceptance_criteria>
  <done>Five new failing test stubs added, all pre-existing tests remain green, RED state confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Write Section 5 and update Summary table (TDD GREEN)</name>
  <files>docs/ai-providers.md</files>
  <read_first>
    - docs/ai-providers.md — read full current content to identify the exact insertion point (after Section 4's closing `---`, before `## Summary`) and current Summary table rows
    - tests/test_ai_provider_docs.py — re-read to confirm exact strings that tests assert (e.g., `"## 5. Third-Party Claude API"`, `"第三方 Claude 兼容 API"`, `"ANTHROPIC_BASE_URL (optional)"`)
  </read_first>
  <action>
    Edit docs/ai-providers.md with the following two changes:

    **Change 1: Insert Section 5 between the closing `---` after Section 4 and the `## Summary` heading.**

    Insert the following block (replacing nothing — pure insertion):

    ```markdown
    ## 5. Third-Party Claude API / 第三方 Claude 兼容 API

    This system supports any Claude-compatible third-party API endpoint. If you have access to a third-party provider that implements the Anthropic API, you can point `call_claude()` at that endpoint using two optional configuration values: `ANTHROPIC_BASE_URL` and `ANTHROPIC_AUTH_TOKEN`. These are independent of `ANTHROPIC_API_KEY` — you do not need an Anthropic account to use a third-party endpoint.

    本系统支持任何兼容 Claude API 的第三方端点。如果你有一个实现了 Anthropic API 的第三方提供商，可以通过两个可选配置值 `ANTHROPIC_BASE_URL` 和 `ANTHROPIC_AUTH_TOKEN` 将 `call_claude()` 指向该端点。这两个值与 `ANTHROPIC_API_KEY` 独立——使用第三方端点无需 Anthropic 账号。

    **Priority / 优先级：** Environment variables take highest priority. `plan/config.json` fields are the lower-priority fallback. Unset or empty values cause the system to fall back to standard Anthropic SDK defaults.

    **优先级：** 环境变量优先级最高。`plan/config.json` 字段为低优先级回退。未设置或为空时，系统回退到标准 Anthropic SDK 默认行为。

    ### Option 1: Environment Variables / 方式一：环境变量

    Set these environment variables before running any script (or add them as GitHub Secrets — see below):

    在运行任意脚本前设置这两个环境变量（或将其添加为 GitHub Secrets——见下文）：

    ```bash
    export ANTHROPIC_BASE_URL="https://your-provider.example.com/v1"
    export ANTHROPIC_AUTH_TOKEN="your-provider-auth-token"
    ```

    ### Option 2: config.json Fields / 方式二：config.json 字段

    Add the optional fields to `plan/config.json`. These are lower priority than env vars — env vars always override them.

    将可选字段添加到 `plan/config.json`。这些字段优先级低于环境变量——环境变量始终覆盖它们。

    ```json
    {
      "ai_provider": "anthropic",
      "openai_model": "gpt-4o-mini",
      "anthropic_base_url": "https://your-provider.example.com/v1",
      "anthropic_auth_token": "your-provider-auth-token"
    }
    ```

    ### Add to GitHub Secrets / 添加为 GitHub Secrets

    To use a third-party endpoint in CI, add the two values as GitHub Repository Secrets:

    如需在 CI 中使用第三方端点，将两个值添加为 GitHub 仓库 Secret：

    ```
    Settings → Secrets and variables → Actions → New repository secret
      Name:  ANTHROPIC_BASE_URL
      Value: <your third-party endpoint URL>
    ```

    ```
    Settings → Secrets and variables → Actions → New repository secret
      Name:  ANTHROPIC_AUTH_TOKEN
      Value: <your third-party auth token>
    ```

    If these secrets are absent or empty, the system uses standard Anthropic API defaults — no action required for normal usage.

    如果这些 Secret 不存在或为空，系统使用标准 Anthropic API 默认值——正常使用无需任何操作。

    ---

    ```

    **Change 2: Append two rows to the Summary table.**

    The current Summary table ends with:
    ```
    | `OPENAI_API_KEY` | Required when `AI_PROVIDER=openai` or as fallback for Anthropic |
    ```

    Append these two rows immediately after that line:
    ```markdown
    | `ANTHROPIC_BASE_URL` (optional) | Third-party Claude-compatible endpoint URL / 第三方 Claude 兼容端点 URL |
    | `ANTHROPIC_AUTH_TOKEN` (optional) | Auth token for third-party endpoint / 第三方端点认证 token |
    ```

    **Exact strings that MUST be present after the edit (verified by tests):**
    - `## 5. Third-Party Claude API`
    - `第三方 Claude 兼容 API`
    - `anthropic_base_url`
    - `anthropic_auth_token`
    - `ANTHROPIC_BASE_URL`
    - `ANTHROPIC_AUTH_TOKEN`
    - `ANTHROPIC_BASE_URL (optional)`
    - `ANTHROPIC_AUTH_TOKEN (optional)`

    **Do NOT:**
    - Modify Sections 1–4 or the existing Summary rows
    - Name any specific third-party provider
    - Add GitHub Secrets steps to Section 3 (Section 3 is for ANTHROPIC_API_KEY and OPENAI_API_KEY only)
    - Create a separate Chinese section — Chinese paragraphs must immediately follow each English paragraph
  </action>
  <verify>
    <automated>pytest tests/test_ai_provider_docs.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep "## 5. Third-Party Claude API" docs/ai-providers.md` exits 0
    - `grep "第三方 Claude 兼容 API" docs/ai-providers.md` exits 0
    - `grep "anthropic_base_url" docs/ai-providers.md` exits 0
    - `grep "anthropic_auth_token" docs/ai-providers.md` exits 0
    - `grep "ANTHROPIC_BASE_URL (optional)" docs/ai-providers.md` exits 0
    - `grep "ANTHROPIC_AUTH_TOKEN (optional)" docs/ai-providers.md` exits 0
    - Section 5 appears between the last `---` of Section 4 and `## Summary`
    - `pytest tests/test_ai_provider_docs.py -v` exits 0 with all 12 tests PASSED
    - `pytest` (full suite) exits 0
  </acceptance_criteria>
  <done>All 12 tests green, Section 5 present and bilingual, config.json example shows both new fields merged with existing fields, GitHub Secrets sub-section documented with standard code block format, Summary table has two optional rows.</done>
</task>

</tasks>

<verification>
Full suite must be green before phase is complete:

```bash
pytest
```

Manual verification (bilingual readability):
- Read Section 5 prose — confirm each English paragraph is immediately followed by a Chinese paragraph (no separate EN/ZH sections)
- Follow the GitHub Secrets steps in Section 5 — confirm navigation path matches current GitHub UI

Structural checks:
- Section 4 (`## 4. Provider Priority Rule`) is unchanged
- Section 3 (`## 3. Add Keys as GitHub Actions Secrets`) is unchanged (still covers ANTHROPIC_API_KEY and OPENAI_API_KEY only)
- Section 5 is numbered `## 5.` and placed between Section 4's closing `---` and `## Summary`
- No specific third-party provider names appear in Section 5
</verification>

<success_criteria>
1. `pytest tests/test_ai_provider_docs.py -v` exits 0 with 12 tests passed (7 existing + 5 new)
2. `pytest` (full suite) exits 0
3. `docs/ai-providers.md` contains `## 5. Third-Party Claude API / 第三方 Claude 兼容 API`
4. `docs/ai-providers.md` contains a JSON code block with `anthropic_base_url` and `anthropic_auth_token` merged into existing config fields
5. `docs/ai-providers.md` contains GitHub Secrets code blocks for `ANTHROPIC_BASE_URL` and `ANTHROPIC_AUTH_TOKEN` inside Section 5
6. Summary table contains `ANTHROPIC_BASE_URL (optional)` and `ANTHROPIC_AUTH_TOKEN (optional)` rows
7. Sections 1–4 and existing Summary rows are unmodified
</success_criteria>

<output>
After completion, create `.planning/phases/08-third-party-provider-documentation/08-01-SUMMARY.md`
</output>
