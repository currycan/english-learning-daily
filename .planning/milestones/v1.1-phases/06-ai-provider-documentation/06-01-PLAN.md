---
phase: 06-ai-provider-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/ai-providers.md
  - tests/test_ai_provider_docs.py
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - DOCS-04

must_haves:
  truths:
    - "docs/ai-providers.md exists and contains step-by-step instructions for obtaining an OpenAI API key from platform.openai.com"
    - "docs/ai-providers.md contains step-by-step instructions for obtaining an Anthropic API key from console.anthropic.com"
    - "docs/ai-providers.md explains how to store OPENAI_API_KEY and ANTHROPIC_API_KEY as GitHub Actions Repository Secrets"
    - "docs/ai-providers.md states the priority rule: AI_PROVIDER env var overrides ai_provider in plan/config.json"
    - "Automated tests assert all four required content strings are present in the document"
  artifacts:
    - path: "docs/ai-providers.md"
      provides: "Standalone AI provider configuration reference"
      contains: "platform.openai.com, console.anthropic.com, OPENAI_API_KEY, ANTHROPIC_API_KEY, AI_PROVIDER"
    - path: "tests/test_ai_provider_docs.py"
      provides: "Automated content assertions for all four DOCS requirements"
      exports: ["test_docs_file_exists", "test_openai_url_present", "test_anthropic_url_present", "test_github_secrets_documented", "test_priority_rule_documented"]
  key_links:
    - from: "tests/test_ai_provider_docs.py"
      to: "docs/ai-providers.md"
      via: "pathlib.Path read"
      pattern: "ai-providers\\.md"
---

<objective>
Create docs/ai-providers.md — the standalone reference for dual-provider configuration — and the pytest test file that verifies it.

Purpose: Satisfy DOCS-01 through DOCS-04. Any developer can follow this document to configure either provider (or both) with zero prior knowledge of the project.
Output: docs/ai-providers.md (new), tests/test_ai_provider_docs.py (new)
</objective>

<execution_context>
@/Users/andrew/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrew/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ai-provider-documentation/06-RESEARCH.md

<interfaces>
<!-- Key values the executor needs. Extracted from codebase. -->

From scripts/ai_provider.py (lines 7-14, 31-32) — source of truth for priority rule:
```python
# Provider resolution priority:
#     1. AI_PROVIDER env var (if set and non-empty)
#     2. ai_provider field in config dict (defaults to "anthropic")

def resolve_provider(config: dict) -> str:
    provider = os.environ.get("AI_PROVIDER") or config.get("ai_provider", "anthropic")
```

From plan/config.json — confirmed field names and defaults:
```json
{
  "ai_provider": "anthropic",
  "openai_model": "gpt-4o-mini"
}
```

From .github/workflows/daily-content.yml — both secrets in use:
```yaml
env:
  ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
  OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
```

From docs/setup-guide.md — verified GitHub Secrets navigation path:
```
Settings → Secrets and variables → Actions → New repository secret
  Name:  ANTHROPIC_API_KEY
  Value: <your Anthropic API key>
```

Bilingual format from existing docs (English line, blank line, Chinese line):
```markdown
Sign up at [console.anthropic.com](https://console.anthropic.com) and create an API key.

在 [console.anthropic.com](https://console.anthropic.com) 注册并创建 API key。
```

Exact URLs confirmed in research:
- OpenAI key management: https://platform.openai.com/api-keys
- Anthropic console: https://console.anthropic.com
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write pytest content assertions for docs/ai-providers.md</name>
  <files>tests/test_ai_provider_docs.py</files>
  <read_first>
    - tests/test_ai_provider.py (existing test file — read to match file structure, import style, and pytest conventions used in this project)
    - .planning/phases/06-ai-provider-documentation/06-VALIDATION.md (exact grep strings required per requirement ID)
  </read_first>
  <behavior>
    - test_docs_file_exists: Path("docs/ai-providers.md").exists() is True
    - test_openai_url_present: "platform.openai.com" appears in docs/ai-providers.md content (DOCS-01)
    - test_anthropic_url_present: "console.anthropic.com" appears in docs/ai-providers.md content (DOCS-02)
    - test_openai_secret_documented: "OPENAI_API_KEY" appears in docs/ai-providers.md content (DOCS-03)
    - test_anthropic_secret_documented: "ANTHROPIC_API_KEY" appears in docs/ai-providers.md content (DOCS-03)
    - test_priority_rule_env_var: "AI_PROVIDER" appears in docs/ai-providers.md content (DOCS-04)
    - test_priority_rule_config_field: "ai_provider" appears in docs/ai-providers.md content (DOCS-04)
  </behavior>
  <action>
Create tests/test_ai_provider_docs.py. All tests must initially FAIL (RED) because docs/ai-providers.md does not exist yet — this is TDD. Use pathlib.Path resolution relative to the test file's location so tests pass from any working directory (e.g., `Path(__file__).parent.parent / "docs" / "ai-providers.md"`).

Match the import style and fixture approach used in tests/test_ai_provider.py. No external dependencies beyond stdlib pathlib and pytest.

Each test function corresponds to one of the seven behaviors listed above. Use a module-level fixture that reads the file content once and shares it across tests (use pytest fixture with scope="module"). The fixture must raise FileNotFoundError (not silently skip) if the file is absent, so the RED state is clearly visible.
  </action>
  <verify>
    <automated>cd /Users/andrew/study-all && pytest tests/test_ai_provider_docs.py -v 2>&1 | grep -E "FAILED|ERROR|passed|failed"</automated>
  </verify>
  <done>All 7 test functions exist in tests/test_ai_provider_docs.py and ALL FAIL with FileNotFoundError or collection error (RED state confirmed). Running `pytest tests/test_ai_provider_docs.py -v` shows 0 passed, 7 failed.</done>
</task>

<task type="auto">
  <name>Task 2: Create docs/ai-providers.md (GREEN — make all tests pass)</name>
  <files>docs/ai-providers.md</files>
  <read_first>
    - docs/setup-guide.md (full file — match bilingual format: English line, blank line, Chinese line; match heading levels and section structure)
    - docs/configuration.md (full file — match table style and code block formatting)
    - tests/test_ai_provider_docs.py (just written — verify every required string is present in the new doc)
    - scripts/ai_provider.py (lines 1-40 — source of truth for priority rule wording)
  </read_first>
  <action>
Create docs/ai-providers.md with the following exact structure and content. Use bilingual format throughout (English line, blank line, Chinese line), matching docs/setup-guide.md style exactly.

---

# AI Provider Configuration / AI 提供商配置

> English · 中文对照

---

## Overview / 概述

[One paragraph in English explaining that the system supports Claude (Anthropic) and OpenAI as interchangeable content generation backends with automatic fallback. Two API keys are supported: ANTHROPIC_API_KEY and OPENAI_API_KEY.]

[Same paragraph in Chinese.]

---

## 1. Get an OpenAI API Key / 获取 OpenAI API Key

(DOCS-01) Step-by-step instructions. Use this exact URL: https://platform.openai.com/api-keys

Steps must be numbered and include:
1. Visit [platform.openai.com](https://platform.openai.com) and sign in (or create a free account).
2. Navigate to **API keys** in the left sidebar (direct link: [platform.openai.com/api-keys](https://platform.openai.com/api-keys)).
3. Click **Create new secret key**.
4. Give the key a name (e.g., `english-daily`), then click **Create secret key**.
5. Copy the key immediately — it is only shown once. Store it securely (e.g., a password manager).

Each step must have an English instruction and a Chinese translation below it.

Cost note: gpt-4o-mini (~1,400 tokens/day) costs under $0.01/day at current pricing.
费用说明：gpt-4o-mini（每天约 1,400 tokens）按当前定价低于 $0.01/天。

---

## 2. Get an Anthropic API Key / 获取 Anthropic API Key

(DOCS-02) Step-by-step instructions. Use this exact URL: https://console.anthropic.com

Steps must be numbered and include:
1. Visit [console.anthropic.com](https://console.anthropic.com) and sign in (or create an account).
2. Navigate to **API Keys** in the left sidebar.
3. Click **Create Key**.
4. Give the key a name (e.g., `english-daily`), then click **Create Key**.
5. Copy the key immediately — it is only shown once.

Each step must have an English instruction and a Chinese translation below it.

Cost note: claude-haiku-4-5-20251001 (~1,400 tokens/day) costs under $0.01/day.

---

## 3. Add Keys as GitHub Actions Secrets / 添加为 GitHub Actions Secrets

(DOCS-03) Both OPENAI_API_KEY and ANTHROPIC_API_KEY must appear in this section.

Instructions for adding each key. Use the exact navigation path verified in docs/setup-guide.md:

```
Settings → Secrets and variables → Actions → New repository secret
  Name:  OPENAI_API_KEY
  Value: <your OpenAI API key>
```

```
Settings → Secrets and variables → Actions → New repository secret
  Name:  ANTHROPIC_API_KEY
  Value: <your Anthropic API key>
```

Add English explanation and Chinese translation around each code block:
- Both keys are injected as environment variables in the CI workflow.
- Neither key is stored in code or config files.
- 两个密钥均注入为 CI 工作流的环境变量。均不存储在代码或配置文件中。

---

## 4. Provider Priority Rule / 提供商优先级规则

(DOCS-04) Must name both AI_PROVIDER (env var) and ai_provider (config.json field).

Explain the two-level priority using this exact rule (derived from scripts/ai_provider.py lines 7-9):

Priority (highest to lowest):
1. `AI_PROVIDER` environment variable — if set and non-empty, always wins.
2. `ai_provider` field in `plan/config.json` — used when the env var is absent (default: `"anthropic"`).

Include this JSON snippet showing the config field:
```json
{
  "ai_provider": "anthropic",
  "openai_model": "gpt-4o-mini"
}
```

Include this example env var override:
```bash
# Override to OpenAI for one run
AI_PROVIDER=openai python -m scripts.feed_article | python -m scripts.generate_exercises | python -m scripts.commit_content
```

Explain fallback: if the primary provider fails, the system automatically retries with the backup provider — no user action required. Both keys should be configured so fallback works in either direction.

Provide Chinese translation for all paragraphs.

---

## Summary / 配置总结

Include a quick-reference table:

| Secret / 密钥 | Required By / 所需功能 |
|---|---|
| ANTHROPIC_API_KEY | Default provider (anthropic); fallback when OpenAI is primary |
| OPENAI_API_KEY | Required when AI_PROVIDER=openai or as fallback for Anthropic |

---

CRITICAL: Do NOT include real API key values or placeholder keys like sk-abc123. Use `<your_openai_api_key>` style only.
CRITICAL: The document must contain these exact strings (for test assertions):
  - "platform.openai.com"
  - "console.anthropic.com"
  - "OPENAI_API_KEY"
  - "ANTHROPIC_API_KEY"
  - "AI_PROVIDER"
  - "ai_provider"
  </action>
  <verify>
    <automated>cd /Users/andrew/study-all && pytest tests/test_ai_provider_docs.py -v && echo "ALL PASS"</automated>
  </verify>
  <done>All 7 tests in tests/test_ai_provider_docs.py pass (GREEN state). Running `pytest tests/test_ai_provider_docs.py -v` shows 7 passed, 0 failed. docs/ai-providers.md exists and contains all required strings: "platform.openai.com", "console.anthropic.com", "OPENAI_API_KEY", "ANTHROPIC_API_KEY", "AI_PROVIDER", "ai_provider".</done>
</task>

</tasks>

<verification>
Full test suite passes after both tasks:

```bash
cd /Users/andrew/study-all && pytest tests/ -v
```

Smoke checks:
```bash
test -f /Users/andrew/study-all/docs/ai-providers.md && echo "FILE EXISTS"
grep -q 'platform.openai.com' /Users/andrew/study-all/docs/ai-providers.md && echo "DOCS-01 OK"
grep -q 'console.anthropic.com' /Users/andrew/study-all/docs/ai-providers.md && echo "DOCS-02 OK"
grep -q 'OPENAI_API_KEY' /Users/andrew/study-all/docs/ai-providers.md && echo "DOCS-03a OK"
grep -q 'ANTHROPIC_API_KEY' /Users/andrew/study-all/docs/ai-providers.md && echo "DOCS-03b OK"
grep -q 'AI_PROVIDER' /Users/andrew/study-all/docs/ai-providers.md && echo "DOCS-04 OK"
```
</verification>

<success_criteria>
- docs/ai-providers.md exists with all required content for DOCS-01, DOCS-02, DOCS-03, DOCS-04
- tests/test_ai_provider_docs.py has 7 passing tests
- All tests in tests/ pass (no regressions)
- Document follows bilingual format matching docs/setup-guide.md
- No real or placeholder API key values in document (uses angle-bracket style only)
</success_criteria>

<output>
After completion, create `.planning/phases/06-ai-provider-documentation/06-01-SUMMARY.md`
</output>
