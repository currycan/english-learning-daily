---
phase: 02-astro-foundation
plan: 01
subsystem: ui
tags: [astro, tailwindcss, content-collections, github-pages, static-site]

# Dependency graph
requires: []
provides:
  - Astro 6 project in website/ with static output and base='/study-all'
  - Content Collections glob() loader reading content/*.md at build time
  - Lesson list homepage sorted descending with empty state
  - Stub lesson pages at /lesson/YYYY-MM-DD rendering raw Markdown
  - package-lock.json committed for npm ci in CI
affects: [03-reading-ux, 04-github-pages-deploy]

# Tech tracking
tech-stack:
  added:
    - astro@6.0.8
    - tailwindcss@4.2.2
    - "@tailwindcss/vite@4.2.2"
  patterns:
    - Content Collections with glob() loader and z.object({}).passthrough() schema for no-frontmatter Markdown
    - Tailwind 4 via @tailwindcss/vite Vite plugin (not deprecated @astrojs/tailwind)
    - render(entry) free function from astro:content (not deprecated entry.render() method)
    - Explicit sort after getCollection() for deterministic ordering

key-files:
  created:
    - website/astro.config.mjs
    - website/package.json
    - website/package-lock.json
    - website/tsconfig.json
    - website/src/content.config.ts
    - website/src/styles/global.css
    - website/src/layouts/Base.astro
    - website/src/pages/index.astro
    - website/src/pages/lesson/[id].astro
  modified: []

key-decisions:
  - "glob() base: '../content' resolves relative to website/ project root (not src/), pointing to repo-root content/"
  - "Tailwind 4 via @tailwindcss/vite Vite plugin; no tailwind.config.js needed for basic usage"
  - "render(entry) free function (Astro 5+) used instead of deprecated entry.render() method"
  - "site: 'https://currycan.github.io' derived from git remote get-url origin = currycan"

patterns-established:
  - "Pattern: Astro Content Collections with passthrough schema for frontmatter-free Markdown"
  - "Pattern: Always sort getCollection() results explicitly — order is non-deterministic"
  - "Pattern: Internal links use href=/lesson/${id}; Astro auto-prefixes base in .astro files"

requirements-completed: [SITE-03, SITE-04]

# Metrics
duration: 6min
completed: 2026-03-24
---

# Phase 02 Plan 01: Astro Foundation Summary

**Astro 6 static site in website/ with Content Collections glob() loader reading content/*.md at build time, producing a sorted lesson list and stub lesson pages at /lesson/YYYY-MM-DD**

## Performance

- **Duration:** 6 min
- **Started:** 2026-03-24T08:21:31Z
- **Completed:** 2026-03-24T08:27:00Z
- **Tasks:** 2
- **Files modified:** 9

## Accomplishments

- Astro 6 project scaffolded in website/ with Tailwind 4 via @tailwindcss/vite; base='/study-all' for GitHub Pages
- Content Collections configured with glob() loader pointing to repo-root content/ using passthrough schema for no-frontmatter Markdown
- Lesson list homepage (index.astro) reads all entries, sorts descending by YYYY-MM-DD id, with empty state copy per UI-SPEC
- Stub lesson pages ([id].astro) use render(entry) free function and getStaticPaths for full static generation
- npm run build exits 0; dist/index.html and dist/lesson/2026-03-24/index.html generated; 114 Python tests unchanged

## Task Commits

Each task was committed atomically:

1. **Task 1: Scaffold Astro project with Tailwind 4 and Content Collections config** - `7b011c3` (feat)
2. **Task 2: Create lesson list homepage and stub lesson pages, verify build** - `38f853e` (feat)

**Plan metadata:** _(docs commit follows)_

## Files Created/Modified

- `website/astro.config.mjs` - Site config: base='/study-all', site=currycan.github.io, @tailwindcss/vite plugin, static output
- `website/package.json` - Astro 6.0.8, tailwindcss 4.2.2, @tailwindcss/vite 4.2.2 dependencies; dev/build/preview scripts
- `website/package-lock.json` - Lockfile committed for npm ci reproducibility in CI
- `website/tsconfig.json` - Extends astro/tsconfigs/strict (generated by npm create astro)
- `website/src/content.config.ts` - lessons collection: glob({ pattern: '*.md', base: '../content' }), z.object({}).passthrough()
- `website/src/styles/global.css` - @import "tailwindcss" only
- `website/src/layouts/Base.astro` - HTML shell: max-w-[640px] mx-auto px-4 py-8, text-base body
- `website/src/pages/index.astro` - Lesson list: getCollection + sort desc + empty state + #2563eb accent links
- `website/src/pages/lesson/[id].astro` - Stub pages: getStaticPaths + render(lesson) + Base layout

## Decisions Made

- Used `base: '../content'` in glob() loader — resolved relative to website/ project root, not src/. Confirmed correct in Pitfall 5 of RESEARCH.md.
- Tailwind 4 setup requires only `@import "tailwindcss"` in global.css and the Vite plugin — no tailwind.config.js needed.
- Used `render(entry)` free function from `astro:content` (Astro 5+ canonical API) — not the deprecated `entry.render()` method.
- Site URL set to `https://currycan.github.io` derived from `git remote get-url origin`.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

**GitHub Pages must be manually enabled before the site is live.** One-time human action:

- **Location:** Repository Settings > Pages > Build and deployment > Source
- **Action:** Select "GitHub Actions" as the source
- **Why:** `actions/deploy-pages` uploads the artifact successfully, but the site only becomes accessible after this setting is enabled.
- **URL after setup:** `https://currycan.github.io/study-all/`

## Next Phase Readiness

- Build pipeline proven end-to-end: content/*.md → Astro build → dist/ with lesson pages
- website/ is fully isolated from Python scripts (no Python imports, separate CI job needed)
- Phase 03 (reading UX) can now build on this foundation — the stub lesson pages at /lesson/YYYY-MM-DD are ready to be replaced with full reading components
- Phase 04 (GitHub Pages deploy) needs the CI workflow extension documented in RESEARCH.md Pattern 5

---
*Phase: 02-astro-foundation*
*Completed: 2026-03-24*

## Self-Check: PASSED

- All 9 created files exist on disk
- Task commits 7b011c3 and 38f853e verified in git log
- npm run build exits 0; dist/index.html and dist/lesson/2026-03-24/index.html confirmed
- 114 Python tests pass
