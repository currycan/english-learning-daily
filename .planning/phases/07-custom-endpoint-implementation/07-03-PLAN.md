---
phase: 07-custom-endpoint-implementation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/daily-content.yml
autonomous: true
requirements:
  - CONF-04
  - TPROV-04

must_haves:
  truths:
    - "ANTHROPIC_BASE_URL env var is injected into the CI step from secrets.ANTHROPIC_BASE_URL"
    - "ANTHROPIC_AUTH_TOKEN env var is injected into the CI step from secrets.ANTHROPIC_AUTH_TOKEN"
    - "Both new lines are in the step-level env: block, NOT the top-level env: block"
    - "Existing ANTHROPIC_API_KEY and OPENAI_API_KEY lines are unchanged"
  artifacts:
    - path: ".github/workflows/daily-content.yml"
      provides: "CI injects two new optional secrets as env vars for the generate step"
      contains: "ANTHROPIC_BASE_URL: ${{ secrets.ANTHROPIC_BASE_URL }}"
  key_links:
    - from: ".github/workflows/daily-content.yml step env"
      to: "scripts/ai_provider.call_claude"
      via: "os.environ.get('ANTHROPIC_BASE_URL') inside call_claude()"
      pattern: "ANTHROPIC_BASE_URL"
---

<objective>
Add ANTHROPIC_BASE_URL and ANTHROPIC_AUTH_TOKEN to the CI workflow's step-level env block so GitHub Actions injects them as environment variables during content generation.

Purpose: Satisfy CONF-04 — repository secrets ANTHROPIC_BASE_URL and ANTHROPIC_AUTH_TOKEN are available as env vars to call_claude() during CI runs. When the secrets don't exist, GitHub returns empty string, which call_claude() treats as absent (no behavioral change).
Output: Modified .github/workflows/daily-content.yml with two new env lines.
</objective>

<execution_context>
@/Users/andrew/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrew/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-custom-endpoint-implementation/07-CONTEXT.md

<interfaces>
<!-- Current step-level env block in .github/workflows/daily-content.yml (lines 32-35): -->
```yaml
      - name: Generate and commit daily content
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
```

<!-- Target state after this plan: -->
```yaml
      - name: Generate and commit daily content
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          ANTHROPIC_BASE_URL: ${{ secrets.ANTHROPIC_BASE_URL }}
          ANTHROPIC_AUTH_TOKEN: ${{ secrets.ANTHROPIC_AUTH_TOKEN }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
```

<!-- Top-level env block (lines 8-9) — DO NOT TOUCH: -->
```yaml
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ANTHROPIC_BASE_URL and ANTHROPIC_AUTH_TOKEN to CI step env block</name>
  <files>.github/workflows/daily-content.yml</files>

  <read_first>
    - .github/workflows/daily-content.yml — read the FULL file; locate the step-level env: block under "Generate and commit daily content" step (lines 32-35); confirm the top-level env: block at lines 8-9 so you do NOT touch it
  </read_first>

  <action>
In .github/workflows/daily-content.yml, find the step-level env: block under the "Generate and commit daily content" step. It currently reads:

```yaml
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
```

Insert two new lines after ANTHROPIC_API_KEY and before OPENAI_API_KEY:

```yaml
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          ANTHROPIC_BASE_URL: ${{ secrets.ANTHROPIC_BASE_URL }}
          ANTHROPIC_AUTH_TOKEN: ${{ secrets.ANTHROPIC_AUTH_TOKEN }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
```

IMPORTANT:
- These lines go ONLY in the step-level `env:` block (indented under the step), NOT in the top-level `env:` block at the start of the file
- The ordering is: ANTHROPIC_API_KEY, then the two new lines, then OPENAI_API_KEY — alphabetical within the Anthropic group
- Indentation must match the existing lines exactly (10 spaces before the key name)
- Do not add conditional YAML logic — inject unconditionally; GitHub returns "" for missing secrets, and call_claude() treats "" as absent

After editing, validate YAML syntax by running:
  `python -c "import yaml; yaml.safe_load(open('.github/workflows/daily-content.yml'))" && echo "YAML valid"`
  </action>

  <verify>
    <automated>python -c "import yaml; yaml.safe_load(open('.github/workflows/daily-content.yml'))" && echo "YAML valid" && grep -n "ANTHROPIC_BASE_URL\|ANTHROPIC_AUTH_TOKEN" .github/workflows/daily-content.yml</automated>
  </verify>

  <acceptance_criteria>
    - .github/workflows/daily-content.yml contains `ANTHROPIC_BASE_URL: ${{ secrets.ANTHROPIC_BASE_URL }}`
    - .github/workflows/daily-content.yml contains `ANTHROPIC_AUTH_TOKEN: ${{ secrets.ANTHROPIC_AUTH_TOKEN }}`
    - `grep -n "ANTHROPIC_BASE_URL" .github/workflows/daily-content.yml` returns exactly 1 line (the step-level env line)
    - `grep -n "ANTHROPIC_AUTH_TOKEN" .github/workflows/daily-content.yml` returns exactly 1 line (the step-level env line)
    - The top-level env block still contains only `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true` (not modified)
    - `python -c "import yaml; yaml.safe_load(open('.github/workflows/daily-content.yml'))"` exits 0 (valid YAML)
    - `grep -n "ANTHROPIC_API_KEY" .github/workflows/daily-content.yml` still returns the unchanged existing line
    - `grep -n "OPENAI_API_KEY" .github/workflows/daily-content.yml` still returns the unchanged existing line
  </acceptance_criteria>

  <done>daily-content.yml step env block includes both new secret injections. YAML is valid. Existing env vars unchanged.</done>
</task>

</tasks>

<verification>
Run after task completes:
1. `python -c "import yaml; yaml.safe_load(open('.github/workflows/daily-content.yml'))" && echo "YAML valid"`
2. `grep -A6 "name: Generate and commit daily content" .github/workflows/daily-content.yml` — shows all 4 env lines
3. `grep -c "ANTHROPIC_BASE_URL\|ANTHROPIC_AUTH_TOKEN" .github/workflows/daily-content.yml` — outputs 2
</verification>

<success_criteria>
- ANTHROPIC_BASE_URL and ANTHROPIC_AUTH_TOKEN lines present in step-level env block
- YAML parses without error
- No changes to top-level env block
- No changes to ANTHROPIC_API_KEY or OPENAI_API_KEY lines
</success_criteria>

<output>
After completion, create `.planning/phases/07-custom-endpoint-implementation/07-03-SUMMARY.md`
</output>
